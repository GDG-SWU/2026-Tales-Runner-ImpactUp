spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dual_tales?serverTimezone=Asia/Seoul
    username: root
    password: '050204'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update #ERD 자동 수정/업데이트
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect